# Prefect N8N Workflow Editor

Deploy locally:

```
 docker compose -f local.yml up -d
```

Install uv package manager: https://github.com/astral-sh/uv

Install deps with uv:
```
 python -m venv .venv
 source .venv/bin/activate
 uv sync
```
